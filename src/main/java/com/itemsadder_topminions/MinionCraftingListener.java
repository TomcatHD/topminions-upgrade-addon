package com.itemsadder_topminions;

import org.bukkit.event.Listener;

public class MinionCraftingListener implements Listener {
    // This class was previously cancelling all clicks
    // It is now intentionally left empty as crafting is handled in MinionCraftingMenu
}